import 'package:flutter/material.dart';
class SumoPage extends StatelessWidget {
  const SumoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}